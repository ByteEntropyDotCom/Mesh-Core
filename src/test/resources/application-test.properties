# Disable strict infrastructure requirements for unit/integration tests
server.ssl.enabled=false
spring.cloud.discovery.enabled=false
spring.cloud.gateway.discovery.locator.enabled=false

# Use a mock signature for HMAC testing
mesh.security.hmac-secret=test-secret-123